services:
  server:
    build: ./server
    image: server-base
    ports:
      - "8080:8080"
    restart: always
    # env_file: .env
    environment:
      - NODE_ENV=production
      - PORT=8080
    expose:
      - "8080"
  client:
    build: ./app
    image: client-base
    ports:
      - "3000:3000"
    restart: always
    environment:
      - PORT=3000
    expose:
      - "3000"