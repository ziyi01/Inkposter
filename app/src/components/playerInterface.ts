export interface Player {
    playerId: string;
    name: string;
    connection: boolean;
}

export interface PlayerCanvas {
    playerId: string;
    playerName: string;
    canvas: string;
    connection: boolean;
}